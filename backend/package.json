{
  "name": "heatmap-guessr-backend",
  "version": "1.0.0",
  "description": "Backend API for Heatmap Guessr",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "dev": "node --watch src/index.js",
    "start": "node src/index.js",
    "test": "cross-env NODE_OPTIONS=--experimental-vm-modules jest --testMatch=\"**/tests/backend/**/*.test.js\" --rootDir=.."
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "jest": "^29.7.0",
    "supertest": "^6.3.4"
  },
  "jest": {
    "transform": {},
    "testMatch": [
      "**/tests/backend/**/*.test.js"
    ],
    "rootDir": ".."
  }
}
