{
  "name": "heatmap-guessr",
  "version": "1.0.0",
  "description": "Daily TV series heatmap guessing game — monorepo root",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "test:backend": "cross-env NODE_OPTIONS=--experimental-vm-modules npx jest --config jest.config.js",
    "test:frontend": "npm test --prefix frontend",
    "test": "npm run test:backend && npm run test:frontend",
    "fetch-data": "node scripts/fetchShows.js",
    "build-pool": "node scripts/buildPuzzlePool.js",
    "install:all": "npm install && npm install --prefix backend --include=dev && npm install --prefix frontend --include=dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cross-env": "^7.0.3",
    "jest": "^29.7.0",
    "supertest": "^6.3.4"
  }
}
